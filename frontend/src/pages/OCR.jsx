import React from 'react';
import { api } from '../utils/api';

// This is a placeholder. We will build this out.
const OCR = () => {
    return (
        <div className="p-8">
            <h1 className="text-2xl font-bold">OCR - Image to Text</h1>
            <p className="mt-4">This feature is coming soon!</p>
        </div>
    );
};

export default OCR;
